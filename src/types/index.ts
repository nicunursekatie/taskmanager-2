// Define and export the Category type
export type Category = {
    id: string;
    name: string;
    color: string;
  };
  
  // Define and export the Project type
  export type Project = {
    id: string;
    name: string;
    description: string;
  };